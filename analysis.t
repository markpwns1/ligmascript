
-- import "preamble"

-- let MIN_INT = -2147483648
-- let MAX_INT = 2147483647

let main = \. -> 
    -- let body = foldl (range MIN_INT MAX_INT) "" (=> "if(*x == ${@}) *x = ${@ + 2};\n") in
    -- let output = "inline void add_two(int* x) {\n${body}}\n" in
    -- let file = io.open "output.c" "w" in do 
    --     file:write output,
    --     file:close!
    -- end
    !

